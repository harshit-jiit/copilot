version: '3.9'

services:

  django:
    build: .
    container_name: django_app
    command: /app/entrypoint.sh
    volumes:
      - .:/app
      - /var/log/gym-multitenants/django:/app/logs/django
      # - ./logs/django:/app/logs/django
      - static_volume:/app/static
      - media_volume:/app/media
    env_file:
      - /home/ubuntu/apps/.env
    ports:
      - "8000:8000"
    # depends_on:
    #   - redis

  # redis:
  #   image: redis:7
  #   container_name: redis
  #   ports:
  #     - "6379:6379"

  # celery:
  #   build: .
  #   container_name: celery_worker
  #   command: celery -A config worker --loglevel=info
  #   volumes:
  #     - .:/app
  #     - /var/log/gym-multitenants/celery:/app/logs/celery
  #     # - ./logs/celery:/app/logs/celery
  #   env_file:
  #     - /home/ubuntu/apps/.env
  #   depends_on:
  #     - redis
  #     - django

  # celery-beat:
  #   build: .
  #   container_name: celery_beat
  #   command: celery -A config beat --loglevel=info
  #   volumes:
  #     - .:/app
  #     - /var/log/gym-multitenants/celery:/app/logs/celery
  #     # - ./logs/celery:/app/logs/celery
  #   env_file:
  #     - /home/ubuntu/apps/.env
  #   depends_on:
  #     - redis
  #     - django

  # nginx:
  #   image: nginx:1.25
  #   container_name: nginx
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
  #     - static_volume:/app/static
  #     - media_volume:/app/media
  #   depends_on:
  #     - django

volumes:
  static_volume:
  media_volume:
